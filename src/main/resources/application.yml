block_ip:
  requests:
    amount: 5
    time_window_seconds: 60
server:
  port: ${PORT:8080}
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  endpoint:
    health:
      show-components: always
      show-details: always